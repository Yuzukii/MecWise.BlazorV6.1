@using Microsoft.JSInterop
@using Newtonsoft.Json.Linq
@inject IJSRuntime JSRuntime;

@if (LabelField == null) {
    <p>Loading</p>
}
else {
    @*<div id="@LabelField.ID" class="p-0 @LabelField.StyleClass">
            <div class="col">
                <div class="col px-0">
                    <span style="height:100%; width:100%; font-family:'Helvetica Neue','Segoe UI',Helvetica,Verdana,sans-serif; font-size:1em; padding: 7px 9px 8px 0px">
                        @LabelField.Description
                    </span>

                </div>
            </div>
        </div>*@
    <div id="@(LabelField.ElementID + "_Container")"  class="p-0 col @LabelField.StyleClassWidth">
        <div id="@(LabelField.ElementID + "_SubContainer")" class="col">
            <div id="@LabelField.ElementID" class="field-label @LabelField.StyleClass">
                @((MarkupString)LabelField.Description) 
            </div>
        </div>
    </div>
}

@code {

    [Parameter]
    public FieldLabel LabelField { get; set; }


}

